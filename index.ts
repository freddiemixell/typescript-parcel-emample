const isOpen: boolean = false
const firstName: string = 'Freddie'
const myAge: number = 28
const list: [number, string] = [1,'Name']

enum Job{ WebDev, ProjectManager, SoftwareEngineer }

const job: Job = Job.WebDev